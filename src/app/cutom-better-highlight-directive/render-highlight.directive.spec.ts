import { RenderHighlightDirective } from './render-highlight.directive';

describe('RenderHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
